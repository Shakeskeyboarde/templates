{
  "name": "templates",
  "private": true,
  "workspaces": [
    "bin",
    "lib",
    "vite",
    "vite-lib"
  ],
  "scripts": {
    "test": "npm -ws --if-present test && npm -ws --if-present run build",
    "modernize": "npx npm-check-updates@latest -i --format group -ws --root --peer"
  }
}
