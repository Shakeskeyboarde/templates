{
  "private": true,
  "name": "~~~target.basename~~~",
  "description": "{{{Description}}}",
  "author": {
    "name": "{{{git.user.name}}}",
    "email": "{{{git.user.email}}}"
  },
  "license": "ISC",
  "version": "1.0.0",
  "types": "lib/index.d.ts",
  "main": "lib/index.js",
  "exports": {
    "types": "./lib/index.d.ts",
    "default": "./lib/index.js"
  },
  "bin": "lib/bin.js",
  "sideEffects": false,
  "scripts": {
    "test": "del-cli out && tsc --noEmit && eslint --max-warnings=0 src && jest --no-cache",
    "prepare": "del-cli lib && tsc && chmodx --package",
    "start": "yon run prepare && node lib/bin.js",
    "clean": "del-cli lib out",
    "modernize": "npm-check-updates -u -x @types/node && yon i"
  },
  "devDependencies": {
    "@types/jest": "^27.4.1",
    "@types/node": "^16.11.22",
    "chmodx": "^1.0.0",
    "del-cli": "^4.0.1",
    "eslint": "^8.13.0",
    "eslint-config-rational": "^0.1.7",
    "jest": "^27.5.1",
    "npm-check-updates": "^12.5.9",
    "prettier": "^2.6.2",
    "source-map-support": "^0.5.21",
    "ts-jest": "^27.1.4",
    "typescript": "^4.6.3",
    "yarn-or-npm": "^3.0.1"
  }
}
